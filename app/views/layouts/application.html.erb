<!DOCTYPE html>
<html lang="fr">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />

    <title>Chez les voisins</title>

    <%= stylesheet_link_tag 'application' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
    <%= csrf_meta_tags %>
  </head>

  <body class="startpage" id="white">
  <p>
    <%= link_to 'Déconnexion', destroy_user_session_path, method: :delete, class: 'button' if user_signed_in?%>
  </p>
  <% unless flash.empty? %>
  <div class="row">
    <div class="columns small-12">
      <% flash.each do |key, value| %>
          <div class="alert-box key" data-alert>
            <%= value %>
            <a class="close" href="#">&times;</a>
          </div>
      <% end %>
    </div>
  </div>
  <% end %>
    <div class="row">
      <div class="columns small-12">
        <%= yield %>
      </div>
    </div>
  </body>
</html>
